export enum EntityType {
    ACCOMMODATION = "ACCOMMODATION",
    USER_PROFILE = "USER_PROFILE",
    ROOM = "ROOM",
    REVIEW = "REVIEW",
}

export enum VariantType {
    ORIGINAL = "ORIGINAL",
    THUMBNAIL = "THUMBNAIL",
    WEBP = "WEBP",
    OPTIMIZED = "OPTIMIZED",
}
